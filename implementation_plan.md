# iPAS AI 應用規劃師互動式練習題庫設計

我們已成功將 12 份 PDF 中的所有試題（每份 50 題，共 600 題）乾淨地解析並存成 `exams_data.json`。
本階段的目標是為網頁系統新增 **「模擬測驗與試題練習」 模式**，讓使用者可以依序做題、隱藏答案、即時點擊對答，並保留閱讀原始 PDF 的切換功能。

---

## 規劃的設計與功能

1. **雙模式切換 (Tabs Layout)**：
   - 當選取某份試題時，右側主面板上方新增模式切換按鈕：
     - 📝 **模擬測驗 (Practice Mode)** [預設開啟]
     - 📄 **閱讀 PDF (PDF Viewer)**
2. **互動式題目呈現**：
   - 題目依序 (1~50 題) 排版顯示。
   - 選項 (A, B, C, D) 呈現為可點擊按鈕，滑鼠懸停時有漸變高亮效果。
   - **隱藏答案與即時對答**：
     - 預設隱藏正確答案。
     - 使用者點擊任一選項時，即時比對：
       - 若選對：該選項顯示**綠色高亮與打勾圖示**。
       - 若選錯：選錯項顯示**紅色高亮與叉叉圖示**，並同時將**正確答案以綠色高亮標示**。
     - 每題下方配有「直接顯示答案 (Reveal Answer)」按鈕。
3. **即時數據統計**：
   - 測驗區頂端顯示目前的答題進度，例如：「已答 12/50 題 | 答對 10 題 | 答對率 83%」。
4. **題目收藏/標記功能 (Star/Bookmark)**：
   - 使用者可以「星號標記」不熟悉的題目，資料儲存於 LocalStorage，方便日後單獨複習。
5. **儲存作答紀錄**：
   - 使用者做題的選擇將被保存在記憶體中，切換模式或分頁時不會丟失當前的答題狀態；並提供「重置本次測驗」按鈕。

---

## 預計修改與新增的檔案

### [Component] 前端與邏輯更新

#### [MODIFY] [index.html](file:///Users/rex/Desktop/iPAS/ipas_ai_exams/index.html)
- 在 `viewer-panel` 中新增頂部 Tab 選單：模擬測驗 / 閱讀 PDF。
- 在 `viewer-panel` 內新增 `quiz-container` 區塊，用以動態渲染 HTML 題目卡片。
- 隱藏原有的 iframe 容器，改由 Tab 控制其顯示/隱藏。

#### [MODIFY] [style.css](file:///Users/rex/Desktop/iPAS/ipas_ai_exams/style.css)
- 新增 Tabs 切換樣式（滑動底線、漸變背景）。
- 新增題目卡片樣式：問題文字、選項按鈕 (A, B, C, D) 及其對應的 `correct`（綠）、`incorrect`（紅）狀態樣式。
- 新增測驗進度儀表板（答對率、進度條）。
- 新增收藏星號的視覺效果。

#### [MODIFY] [app.js](file:///Users/rex/Desktop/iPAS/ipas_ai_exams/app.js)
- 在初始化時載入 `exams_data.json`。
- 實作 Tab 切換邏輯。
- 實作題目動態渲染（依序產出 50 題，並建立對應的點擊作答事件）。
- 實作答題狀態管理（紀錄使用者的選擇、統計答對率）。
- 實作題目星號收藏的 LocalStorage 存取與篩選。

---

## 驗證計劃

### 模擬測驗功能驗證
1. 點擊側邊欄任一試題，確認預設載入「模擬測驗」介面且答案為隱藏狀態。
2. 點擊某題的 (A) 選項：
   - 確認介面即時回饋正誤。
   - 確認無法重複點擊其他選項（锁定單題作答）。
3. 點擊「顯示答案」按鈕，確認能正確揭曉該題正確答案。
4. 點擊「閱讀 PDF」頁籤，確認能順利切換回原始 PDF 視圖，再切換回測驗時，原先的答題進度仍被保留。
5. 點擊「星號收藏」，重整網頁後確認收藏狀態依舊保留。
